#include <iostream>
using namespace std;

int main() {
    int a,b,count=0;
    cin>>a>>b;
    while(a--){
        char c;
        int d;
        cin>>c>>d;
        if(c=='+')b=b+d;
        else if(b>=d)b=b-d;
        else count++;
    }
    cout<<b<<" "<<count<<endl;

    return 0;
}
