#include <bits/stdc++.h>
using namespace std;
int main() {
    
    string s,t;
    cin>>s>>t;
    int count=0,j=0;
    for(int i=0;i<t.length();i++){
        if(s[j]==t[i]){
            count++;
            j++;
        }
    }
    cout<<count+1<<endl;

    return 0;
}
