#include <bits/stdc++.h>
using namespace std;
 
int main() {
    int t;
    cin>>t;
    while(t--){
        vector<string>v;
        for(int i=0;i<8;i++){
            string s;
            cin>>s;
            v.push_back(s);
        }
        string s;
        for(int i=0;i<8;i++){
            for(int j=0;j<8;j++){
                if(v[i][j] != '.'){
                    s+=v[i][j];
                }
                
            }
        }
        cout<<s<<endl;
    }
  
 
    return 0;
}
