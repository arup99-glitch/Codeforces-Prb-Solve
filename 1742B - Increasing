#include <bits/stdc++.h>
using namespace std;

int main() {
   int t;
   cin>>t;
   while(t--){
       int n;
       cin>>n;
       int v[n];
       for(int i=0;i<n;i++)
       cin>>v[i];
       sort(v,v+n);
       int count=1;
       for(int i=0;i<n-1;i++){
           if(v[i]!=v[i+1])count++;
       }
       if(count==n)cout<<"YES"<<endl;
       else cout<<"NO"<<endl;
       }
       return 0;
}
