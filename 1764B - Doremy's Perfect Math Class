#include<bits/stdc++.h>
using  namespace  std;
 
int main(){
     int n,t,g;
     cin>>t;
     while(t--){
     cin>>n;
      g=0;
     vector<int>arr(n);
     for(int i=0;i<=n-1;i++){
        cin>>arr[i];
         g=__gcd(g,arr[i]);
     }
     cout<<arr[n-1]/g<<endl;
     }
    return 0;
}
